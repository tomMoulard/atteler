<!-- Code generated by `go generate ./internal/docgen`; DO NOT EDIT. -->

Every CLI option, one section each. There are 414 options. Flags combine with
the grouped domain commands (before or after the subcommand). Boolean flags
default to off unless noted; omitted values are not sent to providers.

### `--agent`

Agent name to use for prompts

- **Type:** string
- **Default:** `""`

```sh
atteler --agent <value>
```

### `--agent-memory-agent`

Agent namespace for per-agent vector memory; defaults to --agent

- **Type:** string
- **Default:** `""`

```sh
atteler --agent-memory-agent <value>
```

### `--agent-memory-compact`

Remove expired documents from per-agent vector memory and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --agent-memory-compact
```

### `--agent-memory-delete`

Delete one document ID from one agent's vector memory and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --agent-memory-delete <value>
```

### `--agent-memory-index`

File to add to one agent's vector memory (repeatable or comma-separated)

- **Type:** value

```sh
atteler --agent-memory-index <value>
```

### `--agent-memory-limit`

Maximum per-agent memory search results

- **Type:** value

```sh
atteler --agent-memory-limit <value>
```

### `--agent-memory-migrate`

Explicitly migrate and re-vectorize a per-agent memory store

- **Type:** bool
- **Default:** `false`

```sh
atteler --agent-memory-migrate
```

### `--agent-memory-search`

Search one agent's vector memory and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --agent-memory-search <value>
```

### `--agent-memory-store`

JSON store for per-agent vector memory

- **Type:** string
- **Default:** `""`

```sh
atteler --agent-memory-store <value>
```

### `--agent-memory-ttl-seconds`

Expire newly indexed agent memory after this many seconds

- **Type:** value

```sh
atteler --agent-memory-ttl-seconds <value>
```

### `--agent-performance-summary`

Summarize recorded agent performance across saved sessions and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --agent-performance-summary
```

### `--allow-operation`

Permission operation to allow (repeatable/comma-separated): read, write, execute, network, credential_access, git_mutation, merge_delete

- **Type:** value

```sh
atteler --allow-operation <value>
```

### `--area`

Focus area for --scout-run recommendations

- **Type:** string
- **Default:** `""`

```sh
atteler --area <value>
```

### `--artifact-kind`

Kind for --record-artifact

- **Type:** string
- **Default:** `""`

```sh
atteler --artifact-kind <value>
```

### `--artifact-logical-path`

Logical target path/decision for --record-artifact conflict detection

- **Type:** string
- **Default:** `""`

```sh
atteler --artifact-logical-path <value>
```

### `--artifact-review-status`

Review status for --record-artifact

- **Type:** string
- **Default:** `""`

```sh
atteler --artifact-review-status <value>
```

### `--artifact-summary`

Summary for --record-artifact

- **Type:** string
- **Default:** `""`

```sh
atteler --artifact-summary <value>
```

### `--async-plan`

Print dependency-aware async task batches and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --async-plan
```

### `--async-run`

Execute dependency-aware async tasks by spawning Atteler sub-agents and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --async-run
```

### `--async-task`

Task spec for --async-plan/--async-run: id|agent|prompt|dep1+dep2 (repeatable or comma-separated)

- **Type:** value

```sh
atteler --async-task <value>
```

### `--auto-max-depth`

Maximum recursion depth for --auto self-forking before auto mode is suppressed

- **Type:** int
- **Default:** `2`

```sh
atteler --auto-max-depth <n>
```

### `--autonomy`

Risk-based autonomy for agent actions: low, medium, high, or full

- **Type:** value

```sh
atteler --autonomy <value>
```

### `--autoresearch`

Shortcut for --auto=autoresearch --worktree --headless --autonomy high with a positional mission prompt

- **Type:** bool
- **Default:** `false`

```sh
atteler --autoresearch
```

### `--base`

Base branch override for --issue-implement

- **Type:** string
- **Default:** `""`

```sh
atteler --base <value>
```

### `--bash`

Run an explicit local bash command and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --bash <value>
```

### `--bash-dir`

Working directory for --bash

- **Type:** string
- **Default:** `""`

```sh
atteler --bash-dir <value>
```

### `--bash-timeout-seconds`

Timeout in seconds for --bash

- **Type:** value

```sh
atteler --bash-timeout-seconds <value>
```

### `--cancel-headless`

Cancel one live headless run by headless ID and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --cancel-headless <value>
```

### `--cleanup-headless`

Remove terminal headless runs older than --headless-max-age and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --cleanup-headless
```

### `--code-cycles`

List Go import graph cycles for the current repository and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-cycles
```

### `--code-deps`

List direct Go import graph dependencies for this file path or import path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-deps <value>
```

### `--code-file`

Print Go package, symbols, and imports for one file and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file <value>
```

### `--code-file-import-path`

Check/list one Go file import as path:import and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-import-path <value>
```

### `--code-file-import-prefix`

List imports for one Go file matching path:prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-import-prefix <value>
```

### `--code-file-imports`

List imports for one Go file and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-imports <value>
```

### `--code-file-symbol`

List Go symbols for one file and exact name as path:name and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-symbol <value>
```

### `--code-file-symbol-kind`

List Go symbols for one file and kind as path:kind and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-symbol-kind <value>
```

### `--code-file-symbol-prefix`

List Go symbols for one file and name prefix as path:prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-symbol-prefix <value>
```

### `--code-file-symbol-summary`

List symbol kind counts for one Go file and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-symbol-summary <value>
```

### `--code-file-symbols`

List symbols for one Go file and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-file-symbols <value>
```

### `--code-files`

List Go files with package, import, and symbol counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-files
```

### `--code-impact`

List Go files that directly or transitively import this path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-impact <value>
```

### `--code-import-file-summary`

List Go files with import counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-import-file-summary
```

### `--code-import-path`

List Go files that directly import this import path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-path <value>
```

### `--code-import-path-file-summary`

List Go files with import counts for one exact import path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-path-file-summary <value>
```

### `--code-import-path-package-summary`

List Go packages with file counts for one exact import path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-path-package-summary <value>
```

### `--code-import-path-summary`

Summarize file usage count for one exact import path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-path-summary <value>
```

### `--code-import-prefix`

List Go files that directly import paths with this prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-prefix <value>
```

### `--code-import-prefix-file-summary`

List Go files with import counts for paths matching this prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-prefix-file-summary <value>
```

### `--code-import-prefix-package-summary`

List Go packages with file and import counts for paths matching this prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-prefix-package-summary <value>
```

### `--code-import-prefix-summary`

Summarize import usage counts for paths with this prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-import-prefix-summary <value>
```

### `--code-import-summary`

List Go import paths with usage counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-import-summary
```

### `--code-imports`

List Go import edges in the current repository and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-imports
```

### `--code-language`

Filter --code-query by language, such as go or python

- **Type:** string
- **Default:** `""`

```sh
atteler --code-language <value>
```

### `--code-layers`

List topological Go import graph layers for the current repository and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-layers
```

### `--code-limit`

Maximum code-intel rows to emit for list-style results

- **Type:** value

```sh
atteler --code-limit <value>
```

### `--code-offset`

Number of code-intel rows to skip before emitting list-style results

- **Type:** value

```sh
atteler --code-offset <value>
```

### `--code-package`

List Go files and symbol counts for one package and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package <value>
```

### `--code-package-import-file-summary`

List files in one Go package with import counts and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-import-file-summary <value>
```

### `--code-package-import-files`

List files in one package importing an exact path as package:import and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-import-files <value>
```

### `--code-package-import-path`

List exact import usage for one package as package:import and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-import-path <value>
```

### `--code-package-import-path-file-summary`

List files in one package with import counts for package:import and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-import-path-file-summary <value>
```

### `--code-package-import-prefix`

List import usage for one package and import prefix as package:prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-import-prefix <value>
```

### `--code-package-import-prefix-file-summary`

List files in one package with import counts for paths matching package:prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-import-prefix-file-summary <value>
```

### `--code-package-import-prefix-files`

List files in one package importing paths with prefix as package:prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-import-prefix-files <value>
```

### `--code-package-import-summary`

List Go packages with import counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-package-import-summary
```

### `--code-package-imports`

List import usage counts for one Go package and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-imports <value>
```

### `--code-package-symbol`

List Go symbols for one package and exact name as package:name and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol <value>
```

### `--code-package-symbol-file-summary`

List files in one Go package with symbol counts and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol-file-summary <value>
```

### `--code-package-symbol-kind`

List Go symbols for one package and kind as package:kind and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol-kind <value>
```

### `--code-package-symbol-kind-file-summary`

List files in one Go package with symbol counts for package:kind and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol-kind-file-summary <value>
```

### `--code-package-symbol-list`

List Go symbols declared in one package and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol-list <value>
```

### `--code-package-symbol-name-file-summary`

List files in one Go package with symbol counts for package:name and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol-name-file-summary <value>
```

### `--code-package-symbol-prefix`

List Go symbols for one package and name prefix as package:prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol-prefix <value>
```

### `--code-package-symbol-prefix-file-summary`

List files in one Go package with symbol counts for package:prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbol-prefix-file-summary <value>
```

### `--code-package-symbols`

List symbol kind counts for one Go package and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-package-symbols <value>
```

### `--code-packages`

List Go packages with file and symbol counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-packages
```

### `--code-query`

Query the shared code-intelligence index: files, symbols, definitions, references, diagnostics, or relationships; optionally append :value

- **Type:** string
- **Default:** `""`

```sh
atteler --code-query <value>
```

### `--code-rdeps`

List direct Go import graph reverse dependencies for this file path or import path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-rdeps <value>
```

### `--code-reachable`

List Go import graph nodes reachable from this file path or import path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-reachable <value>
```

### `--code-summary`

Print compact Go code index and import graph counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-summary
```

### `--code-symbol`

Find Go symbols by exact name in the current repository and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol <value>
```

### `--code-symbol-file-summary`

List Go files with symbol counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-symbol-file-summary
```

### `--code-symbol-kind`

List Go symbols by kind (func, method, type, const, var) and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-kind <value>
```

### `--code-symbol-kind-file-summary`

List Go files with symbol counts for one kind and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-kind-file-summary <value>
```

### `--code-symbol-kind-package-summary`

List Go packages with file and symbol counts for one kind and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-kind-package-summary <value>
```

### `--code-symbol-name-file-summary`

List Go files with symbol counts for this exact name and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-name-file-summary <value>
```

### `--code-symbol-name-package-summary`

List Go packages with file and symbol counts for this exact name and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-name-package-summary <value>
```

### `--code-symbol-prefix`

Find Go symbols by name prefix in the current repository and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-prefix <value>
```

### `--code-symbol-prefix-file-summary`

List Go files with symbol counts for names matching this prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-prefix-file-summary <value>
```

### `--code-symbol-prefix-package-summary`

List Go packages with file and symbol counts for names matching this prefix and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --code-symbol-prefix-package-summary <value>
```

### `--code-symbol-summary`

List Go symbol kinds with counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --code-symbol-summary
```

### `--command-surface-docs`

Render the inspectable CLI command surface as Markdown and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --command-surface-docs
```

### `--command-surface-json`

Dump the inspectable CLI command surface as JSON and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --command-surface-json
```

### `--competitor`

Alias for --competitors

- **Type:** value

```sh
atteler --competitor <value>
```

### `--competitors`

Competitor names or URLs for --scout-run inspiration (repeatable or comma-separated)

- **Type:** value

```sh
atteler --competitors <value>
```

### `--config`

Additional YAML/JSON config file path(s); same format as ATTELER_CONFIG

- **Type:** string
- **Default:** `""`

```sh
atteler --config <value>
```

### `--config-migrate`

Migrate existing Atteler config/state files to the current schema and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --config-migrate
```

### `--config-report`

Print redacted config diagnostics for issue reports and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --config-report
```

### `--context-pack-file`

Compact a role-prefixed transcript file and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --context-pack-file <value>
```

### `--context-pack-tokens`

Maximum estimated tokens for --context-pack-file; capped by --model context window when known

- **Type:** value

```sh
atteler --context-pack-tokens <value>
```

### `--deny-operation`

Permission operation to deny (repeatable/comma-separated): read, write, execute, network, credential_access, git_mutation, merge_delete

- **Type:** value

```sh
atteler --deny-operation <value>
```

### `--describe-agent`

Print a configured agent as YAML and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --describe-agent <value>
```

### `--describe-plugin`

Print a configured plugin manifest as YAML and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --describe-plugin <value>
```

### `--doctor`

Print local readiness diagnostics and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --doctor
```

### `--doctor-offline`

Print offline readiness diagnostics without provider health checks and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --doctor-offline
```

### `--eval-approve-golden-update`

Confirm reviewed golden updates for --eval-update-golden

- **Type:** bool
- **Default:** `false`

```sh
atteler --eval-approve-golden-update
```

### `--eval-assertions`

YAML/JSON structured assertion file for --eval-output

- **Type:** string
- **Default:** `""`

```sh
atteler --eval-assertions <value>
```

### `--eval-exit-code`

Actual process exit code available to structured eval assertions

- **Type:** value

```sh
atteler --eval-exit-code <value>
```

### `--eval-expected`

Expected text for --eval-output

- **Type:** string
- **Default:** `""`

```sh
atteler --eval-expected <value>
```

### `--eval-expected-file`

Expected output file for --eval-output

- **Type:** string
- **Default:** `""`

```sh
atteler --eval-expected-file <value>
```

### `--eval-fixture-dir`

Directory of structured eval fixtures to discover and run

- **Type:** string
- **Default:** `""`

```sh
atteler --eval-fixture-dir <value>
```

### `--eval-json`

Print machine-readable eval report JSON

- **Type:** bool
- **Default:** `false`

```sh
atteler --eval-json
```

### `--eval-mode`

Eval mode: exact, contains, or normalized

- **Type:** string
- **Default:** `string(atteval.ModeContains)`

```sh
atteler --eval-mode <value>
```

### `--eval-output`

Actual output file to compare and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --eval-output <value>
```

### `--eval-report`

Write machine-readable eval report JSON to this path

- **Type:** string
- **Default:** `""`

```sh
atteler --eval-report <value>
```

### `--eval-update-golden`

Update golden files from actual output

- **Type:** bool
- **Default:** `false`

```sh
atteler --eval-update-golden
```

### `--evaluation-agent-version`

Agent version identifier for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-agent-version <value>
```

### `--evaluation-confidence`

Confidence for --record-evaluation (0..1)

- **Type:** value

```sh
atteler --evaluation-confidence <value>
```

### `--evaluation-cost`

Cost for --record-evaluation

- **Type:** value

```sh
atteler --evaluation-cost <value>
```

### `--evaluation-difficulty`

Difficulty label for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-difficulty <value>
```

### `--evaluation-duration-millis`

Duration in milliseconds for --record-evaluation

- **Type:** value

```sh
atteler --evaluation-duration-millis <value>
```

### `--evaluation-evaluator`

Evaluator identity for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-evaluator <value>
```

### `--evaluation-expected-outcome`

Expected outcome for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-expected-outcome <value>
```

### `--evaluation-fixture-version`

Fixture version for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-fixture-version <value>
```

### `--evaluation-flake-count`

Flake count for --record-evaluation

- **Type:** value

```sh
atteler --evaluation-flake-count <value>
```

### `--evaluation-input-tokens`

Input tokens for --record-evaluation

- **Type:** value

```sh
atteler --evaluation-input-tokens <value>
```

### `--evaluation-model`

Model identifier for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-model <value>
```

### `--evaluation-notes`

Notes for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-notes <value>
```

### `--evaluation-outcome`

Outcome for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-outcome <value>
```

### `--evaluation-output-tokens`

Output tokens for --record-evaluation

- **Type:** value

```sh
atteler --evaluation-output-tokens <value>
```

### `--evaluation-pass-rate`

Pass rate for --record-evaluation (0..1)

- **Type:** value

```sh
atteler --evaluation-pass-rate <value>
```

### `--evaluation-provider`

Provider identifier for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-provider <value>
```

### `--evaluation-reference`

Reference for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-reference <value>
```

### `--evaluation-report`

Machine-readable eval report JSON to populate --record-evaluation metrics

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-report <value>
```

### `--evaluation-rubric-version`

Versioned rubric identifier for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-rubric-version <value>
```

### `--evaluation-score`

Score for --record-evaluation

- **Type:** value

```sh
atteler --evaluation-score <value>
```

### `--evaluation-source`

Provenance for --record-evaluation: human, harness, or ci

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-source <value>
```

### `--evaluation-task-type`

Task class for --record-evaluation

- **Type:** string
- **Default:** `""`

```sh
atteler --evaluation-task-type <value>
```

### `--evaluation-total-tokens`

Total tokens for --record-evaluation

- **Type:** value

```sh
atteler --evaluation-total-tokens <value>
```

### `--explain-config`

Print merged config values with per-field provenance and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --explain-config
```

### `--explain-config-field`

Limit --explain-config output to this field path prefix

- **Type:** string
- **Default:** `""`

```sh
atteler --explain-config-field <value>
```

### `--explain-model-resolution`

Explain how a model ID resolves to a provider and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --explain-model-resolution <value>
```

### `--export-format`

Session export format: markdown, json, private-markdown, private-json, issue, issue-json; run exports also accept text, replay, or resume

- **Type:** string
- **Default:** `markdown`

```sh
atteler --export-format <value>
```

### `--export-run`

Multi-agent run ID, latest, review, or speculation to export from the selected session

- **Type:** string
- **Default:** `""`

```sh
atteler --export-run <value>
```

### `--export-session`

Session ID or path to export and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --export-session <value>
```

### `--failure-commit`

Commit or reference associated with --record-failure

- **Type:** string
- **Default:** `""`

```sh
atteler --failure-commit <value>
```

### `--failure-reason`

Reason for --record-failure

- **Type:** string
- **Default:** `""`

```sh
atteler --failure-reason <value>
```

### `--failure-severity`

Severity for --record-failure

- **Type:** string
- **Default:** `""`

```sh
atteler --failure-severity <value>
```

### `--failure-task-type`

Task class for --record-failure

- **Type:** string
- **Default:** `""`

```sh
atteler --failure-task-type <value>
```

### `--feedback-apply-config`

Record feedback proposals from the selected session as pending guidance in this agent config file

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-apply-config <value>
```

### `--feedback-approve-agent`

Agent name for --feedback-approve-config

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-approve-agent <value>
```

### `--feedback-approve-config`

Approve one pending feedback guidance record in this agent config file

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-approve-config <value>
```

### `--feedback-approve-id`

Guidance ID for --feedback-approve-config

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-approve-id <value>
```

### `--feedback-history`

Append feedback apply/rollback decisions to this history log

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-history <value>
```

### `--feedback-proposals`

Derive agent improvement proposals from the selected session and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --feedback-proposals
```

### `--feedback-rollback-agent`

Agent name for --feedback-rollback-config

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-rollback-agent <value>
```

### `--feedback-rollback-config`

Roll back one feedback guidance record in this agent config file

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-rollback-config <value>
```

### `--feedback-rollback-id`

Guidance ID for --feedback-rollback-config

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-rollback-id <value>
```

### `--feedback-rollback-reason`

Optional rollback reason for --feedback-rollback-config

- **Type:** string
- **Default:** `""`

```sh
atteler --feedback-rollback-reason <value>
```

### `--generate-tasks`

Write tasks.generated.yaml for research/scout follow-up work

- **Type:** bool
- **Default:** `false`

```sh
atteler --generate-tasks
```

### `--git-history-limit`

Maximum --git-history-search results

- **Type:** value

```sh
atteler --git-history-limit <value>
```

### `--git-history-search`

Search local git history subjects/files/authors and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --git-history-search <value>
```

### `--headless`

Run one-shot prompt without TUI output while recording headless metadata and logs

- **Type:** bool
- **Default:** `false`

```sh
atteler --headless
```

### `--headless-id`

Set the file-name run ID for a --headless one-shot run so automation can monitor or cancel it

- **Type:** string
- **Default:** `""`

```sh
atteler --headless-id <value>
```

### `--headless-max-age`

Duration age filter for --list-headless or retention window for --cleanup-headless, e.g. 24h

- **Type:** string
- **Default:** `""`

```sh
atteler --headless-max-age <value>
```

### `--headless-private-log`

Store unredacted headless prompts, errors, event summaries, and logs; use only for local private runs

- **Type:** bool
- **Default:** `false`

```sh
atteler --headless-private-log
```

### `--headless-status`

Comma-separated statuses for --list-headless; defaults to all statuses

- **Type:** string
- **Default:** `""`

```sh
atteler --headless-status <value>
```

### `--incident-apply-fix`

Run the redacted incident fix prompt through the selected Atteler agent before validation and PR creation

- **Type:** bool
- **Default:** `false`

```sh
atteler --incident-apply-fix
```

### `--incident-diagnose`

Diagnose a production incident from Sentry, MCP, or a redacted JSON file and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --incident-diagnose
```

### `--incident-file`

Load redacted incident JSON from this file instead of a live integration

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-file <value>
```

### `--incident-mcp-manifest`

MCP manifest YAML/JSON containing an observability incident tool

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-mcp-manifest <value>
```

### `--incident-mcp-server`

MCP server name for incident diagnose

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-mcp-server <value>
```

### `--incident-mcp-tool`

MCP tool name that returns normalized incident JSON

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-mcp-tool <value>
```

### `--incident-mcp-tool-args`

JSON object arguments for --incident-mcp-tool

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-mcp-tool-args <value>
```

### `--incident-open-pr`

After --incident-apply-fix and at least one validation command, open a GitHub PR with the redacted incident report using gh pr create

- **Type:** bool
- **Default:** `false`

```sh
atteler --incident-open-pr
```

### `--incident-pr-body`

Write a redacted PR body template for the diagnosed incident

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-pr-body <value>
```

### `--incident-ref`

Generic incident reference for MCP/file-backed diagnose

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-ref <value>
```

### `--incident-report`

Write the redacted incident diagnosis report to this path

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-report <value>
```

### `--incident-repro-command`

Safe local command to attempt reproducing the incident

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-repro-command <value>
```

### `--incident-sentry-event`

Specific Sentry event ID to fetch for stack traces

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-sentry-event <value>
```

### `--incident-sentry-org`

Sentry organization slug for --sentry; defaults to SENTRY_ORG

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-sentry-org <value>
```

### `--incident-sentry-token-env`

Environment variable containing the Sentry auth token; defaults to SENTRY_AUTH_TOKEN, then SENTRY_ACCESS_TOKEN, then SENTRY_TOKEN

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-sentry-token-env <value>
```

### `--incident-sentry-url`

Sentry base URL for --sentry; defaults to SENTRY_BASE_URL, SENTRY_HOST, or https://sentry.io

- **Type:** string
- **Default:** `""`

```sh
atteler --incident-sentry-url <value>
```

### `--incident-timeout-seconds`

Timeout in seconds for incident integration, repro, validation, and PR commands

- **Type:** value

```sh
atteler --incident-timeout-seconds <value>
```

### `--incident-validation-command`

Validation command to run after diagnosis (repeatable)

- **Type:** value

```sh
atteler --incident-validation-command <value>
```

### `--init-config`

Write a starter YAML config to this path without overwriting

- **Type:** string
- **Default:** `""`

```sh
atteler --init-config <value>
```

### `--init-rtk-plugin`

Write an RTK plugin scaffold to this directory and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --init-rtk-plugin <value>
```

### `--issue-implement`

Implement one tracker issue through Symphony and exit

- **Type:** value

```sh
atteler --issue-implement <value>
```

### `--issue-workflow`

WORKFLOW.md path for --issue-implement; defaults to ./WORKFLOW.md

- **Type:** string
- **Default:** `""`

```sh
atteler --issue-workflow <value>
```

### `--json`

Emit JSON for commands with structured output, such as code-intel or config doctor-offline

- **Type:** bool
- **Default:** `false`

```sh
atteler --json
```

### `--list-agents`

List configured agents and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-agents
```

### `--list-artifacts`

List artifacts recorded on the selected session and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-artifacts
```

### `--list-config-paths`

List config files in load order and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-config-paths
```

### `--list-evaluations`

List agent evaluations recorded on the selected session and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-evaluations
```

### `--list-failures`

List negative-knowledge records on the selected session and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-failures
```

### `--list-headless`

List headless runs and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-headless
```

### `--list-hook-events`

List supported lifecycle hook event types and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-hook-events
```

### `--list-hook-events-json`

List supported lifecycle hook event types as JSON and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-hook-events-json
```

### `--list-known-models`

List built-in provider/model IDs without API calls and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-known-models
```

### `--list-messages`

List compact message records on the selected session and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-messages
```

### `--list-models`

List available models and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-models
```

### `--list-plugins`

List configured local plugin manifests and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-plugins
```

### `--list-providers`

List built-in provider names without API calls and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-providers
```

### `--list-runs`

List review/speculation multi-agent runs recorded on the selected session and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-runs
```

### `--list-session-tags`

List saved session tags with counts and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-session-tags
```

### `--list-sessions`

List saved sessions and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-sessions
```

### `--list-sessions-tag`

Filter --list-sessions to sessions containing this exact tag

- **Type:** string
- **Default:** `""`

```sh
atteler --list-sessions-tag <value>
```

### `--list-worktrees`

List active atteler worktrees and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --list-worktrees
```

### `--lsp-arg`

Language server argument for LSP commands (repeatable)

- **Type:** value

```sh
atteler --lsp-arg <value>
```

### `--lsp-command`

Language server command for --lsp-symbols or --lsp-workspace-symbols

- **Type:** string
- **Default:** `""`

```sh
atteler --lsp-command <value>
```

### `--lsp-file`

Source file to inspect with --lsp-symbols

- **Type:** string
- **Default:** `""`

```sh
atteler --lsp-file <value>
```

### `--lsp-language`

Language ID for --lsp-symbols; inferred from --lsp-file when omitted

- **Type:** string
- **Default:** `""`

```sh
atteler --lsp-language <value>
```

### `--lsp-root`

Workspace root for --lsp-symbols

- **Type:** string
- **Default:** `""`

```sh
atteler --lsp-root <value>
```

### `--lsp-symbols`

Request document symbols from an external language server and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --lsp-symbols
```

### `--lsp-workspace-symbols`

Query workspace symbols from an external language server and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --lsp-workspace-symbols <value>
```

### `--max-input-tokens`

Hard cap on estimated input tokens before an LLM call

- **Type:** value

```sh
atteler --max-input-tokens <value>
```

### `--max-tokens`

Override request max output tokens

- **Type:** value

```sh
atteler --max-tokens <value>
```

### `--mcp-capability`

Find servers declaring this capability in --mcp-manifest

- **Type:** string
- **Default:** `""`

```sh
atteler --mcp-capability <value>
```

### `--mcp-manifest`

Validate/list or invoke an MCP manifest YAML/JSON file and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --mcp-manifest <value>
```

### `--mcp-method`

Invoke this JSON-RPC method on --mcp-server

- **Type:** string
- **Default:** `""`

```sh
atteler --mcp-method <value>
```

### `--mcp-params`

JSON params for --mcp-method

- **Type:** string
- **Default:** `""`

```sh
atteler --mcp-params <value>
```

### `--mcp-server`

Server name in --mcp-manifest for --mcp-method or --mcp-tool

- **Type:** string
- **Default:** `""`

```sh
atteler --mcp-server <value>
```

### `--mcp-timeout-seconds`

Timeout in seconds for --mcp-method or --mcp-tool

- **Type:** value

```sh
atteler --mcp-timeout-seconds <value>
```

### `--mcp-tool`

Invoke this MCP tool through tools/call on --mcp-server

- **Type:** string
- **Default:** `""`

```sh
atteler --mcp-tool <value>
```

### `--mcp-tool-args`

JSON object arguments for --mcp-tool

- **Type:** string
- **Default:** `""`

```sh
atteler --mcp-tool-args <value>
```

### `--memory-agent`

Agent name for agent-scoped session memory; defaults to --agent when --memory-scope agent

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-agent <value>
```

### `--memory-compact`

Remove expired documents from a JSON memory store and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --memory-compact
```

### `--memory-delete`

Delete one document ID from a JSON memory store and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-delete <value>
```

### `--memory-global`

Opt in to all saved sessions for memory search/rebuild

- **Type:** bool
- **Default:** `false`

```sh
atteler --memory-global
```

### `--memory-include-session-messages`

Opt in to indexing raw saved-session messages into local memory after redaction

- **Type:** bool
- **Default:** `false`

```sh
atteler --memory-include-session-messages
```

### `--memory-include-worktree-metadata`

Opt in to indexing saved-session worktree paths and branches into local memory after redaction

- **Type:** bool
- **Default:** `false`

```sh
atteler --memory-include-worktree-metadata
```

### `--memory-index`

File to add to memory before saving/searching (repeatable or comma-separated)

- **Type:** value

```sh
atteler --memory-index <value>
```

### `--memory-limit`

Maximum memory search results

- **Type:** value

```sh
atteler --memory-limit <value>
```

### `--memory-list-corpus`

Print memory corpus metadata and document counts

- **Type:** bool
- **Default:** `false`

```sh
atteler --memory-list-corpus
```

### `--memory-migrate`

Explicitly migrate and redact a JSON memory store

- **Type:** bool
- **Default:** `false`

```sh
atteler --memory-migrate
```

### `--memory-purge`

Purge memory documents by session:<id>, tag:<tag>, repo:<path>, or all

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-purge <value>
```

### `--memory-rebuild`

Rebuild --memory-store from the selected memory corpus

- **Type:** bool
- **Default:** `false`

```sh
atteler --memory-rebuild
```

### `--memory-redact`

Custom RE2 regexp to redact from memory before indexing/printing (repeatable)

- **Type:** value

```sh
atteler --memory-redact <value>
```

### `--memory-repo`

Repository/worktree path for repo-scoped memory; defaults to git root or cwd

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-repo <value>
```

### `--memory-retention-days`

Retention window in days for indexed session documents

- **Type:** value

```sh
atteler --memory-retention-days <value>
```

### `--memory-scope`

Memory corpus scope: session, repo, tags, date-range, agent, global, or store

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-scope <value>
```

### `--memory-search`

Search scoped local memory from selected sessions, --memory-store, and --memory-index files

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-search <value>
```

### `--memory-session`

Session ID or path for session-scoped memory

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-session <value>
```

### `--memory-since`

Include sessions updated on/after this date (YYYY-MM-DD or RFC3339)

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-since <value>
```

### `--memory-store`

JSON memory store path to load and/or save

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-store <value>
```

### `--memory-tag`

Session tag for tagged memory scope (repeatable or comma-separated)

- **Type:** value

```sh
atteler --memory-tag <value>
```

### `--memory-ttl-seconds`

Expire newly indexed memory after this many seconds

- **Type:** value

```sh
atteler --memory-ttl-seconds <value>
```

### `--memory-until`

Include sessions updated on/before this date (YYYY-MM-DD or RFC3339)

- **Type:** string
- **Default:** `""`

```sh
atteler --memory-until <value>
```

### `--merge-artifact-max-bytes`

Maximum bytes to read from each --merge-artifacts input

- **Type:** value

```sh
atteler --merge-artifact-max-bytes <value>
```

### `--merge-artifacts`

Write selected-session artifacts as a merged bundle; use '-' for stdout

- **Type:** string
- **Default:** `""`

```sh
atteler --merge-artifacts <value>
```

### `--merge-artifacts-format`

Format for --merge-artifacts: markdown or json

- **Type:** string
- **Default:** `markdown`

```sh
atteler --merge-artifacts-format <value>
```

### `--merge-worktree`

Merge a session worktree back into its base branch and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --merge-worktree <value>
```

### `--merge-worktree-allow-base-mismatch`

Allow manual worktree merge when current branch differs from recorded base

- **Type:** bool
- **Default:** `false`

```sh
atteler --merge-worktree-allow-base-mismatch
```

### `--model`

Model ID to use

- **Type:** string
- **Default:** `""`

```sh
atteler --model <value>
```

### `--model-mode`

Override request model mode (default or fast)

- **Type:** string
- **Default:** `""`

```sh
atteler --model-mode <value>
```

### `--no-auto-merge`

Keep worktree alive on exit (default; overrides configured auto-merge)

- **Type:** bool
- **Default:** `false`

```sh
atteler --no-auto-merge
```

### `--ollama-status`

Print Ollama daemon lifecycle status without starting it and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --ollama-status
```

### `--ollama-stop`

Stop and clean up the Atteler-owned Ollama daemon record and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --ollama-stop
```

### `--once`

Send one prompt and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --once <value>
```

### `--open-pr`

Enable PR publication for --issue-implement

- **Type:** bool
- **Default:** `false`

```sh
atteler --open-pr
```

### `--permission-mode`

Side-effect policy mode: allow, ask, deny, or read-only

- **Type:** string
- **Default:** `""`

```sh
atteler --permission-mode <value>
```

### `--plan-agent`

Explicit agent name to include in --plan-agents (repeatable or comma-separated)

- **Type:** value

```sh
atteler --plan-agent <value>
```

### `--plan-agents`

Plan configured agents for this prompt and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --plan-agents <value>
```

### `--plan-max-agents`

Maximum agents to include in --plan-agents

- **Type:** value

```sh
atteler --plan-max-agents <value>
```

### `--plugin-dry-run`

Describe --run-plugin without executing it

- **Type:** bool
- **Default:** `false`

```sh
atteler --plugin-dry-run
```

### `--plugin-entrypoint`

Entrypoint name for --run-plugin

- **Type:** string
- **Default:** `""`

```sh
atteler --plugin-entrypoint <value>
```

### `--plugin-timeout-seconds`

Timeout in seconds for --run-plugin

- **Type:** value

```sh
atteler --plugin-timeout-seconds <value>
```

### `--print-config-template`

Print a starter YAML config and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --print-config-template
```

### `--prompt-complete`

Suggest local prompt completions with context freshness and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --prompt-complete <value>
```

### `--prompt-complete-limit`

Maximum --prompt-complete suggestions

- **Type:** value

```sh
atteler --prompt-complete-limit <value>
```

### `--prompt-local-only`

Force local prompt completions only; model-backed idle suggestions are opt-in via /suggestions

- **Type:** bool
- **Default:** `false`

```sh
atteler --prompt-local-only
```

### `--reasoning-level`

Override request reasoning level/effort

- **Type:** string
- **Default:** `""`

```sh
atteler --reasoning-level <value>
```

### `--record-artifact`

Record a session artifact path and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --record-artifact <value>
```

### `--record-evaluation`

Record an evaluation for this agent on the selected session and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --record-evaluation <value>
```

### `--record-failure`

Record a failed approach/negative-knowledge note on the selected session and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --record-failure <value>
```

### `--record-response`

Record a one-shot response to this JSON file

- **Type:** string
- **Default:** `""`

```sh
atteler --record-response <value>
```

### `--recover-headless`

Reconcile stale/orphaned/expired headless runs and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --recover-headless
```

### `--replay`

Session ID or path to print and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --replay <value>
```

### `--replay-response`

Replay a recorded one-shot response JSON file without calling an LLM

- **Type:** string
- **Default:** `""`

```sh
atteler --replay-response <value>
```

### `--replay-run`

Multi-agent run ID, latest, review, or speculation to replay without provider calls

- **Type:** string
- **Default:** `""`

```sh
atteler --replay-run <value>
```

### `--research-output`

Directory for --research-run artifacts; defaults to .atteler/runs/research/<run-id>

- **Type:** string
- **Default:** `""`

```sh
atteler --research-output <value>
```

### `--research-run`

Create a local-first cited research run for this question and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --research-run <value>
```

### `--research-source`

Local file/dir or URL to include in --research-run (repeatable or comma-separated)

- **Type:** value

```sh
atteler --research-source <value>
```

### `--resume-run`

Multi-agent run ID, latest, review, or speculation to resume from recorded artifacts without provider calls

- **Type:** string
- **Default:** `""`

```sh
atteler --resume-run <value>
```

### `--retrieval-explain`

Include ranking explanations in --retrieval-search output

- **Type:** bool
- **Default:** `false`

```sh
atteler --retrieval-explain
```

### `--retrieval-filter`

Exact retrieval filter as key=value, e.g. agent=reviewer or source.name=session-id (repeatable)

- **Type:** value

```sh
atteler --retrieval-filter <value>
```

### `--retrieval-include-unsafe`

Include retrieval results not safe for prompt injection

- **Type:** bool
- **Default:** `false`

```sh
atteler --retrieval-include-unsafe
```

### `--retrieval-limit`

Maximum --retrieval-search results

- **Type:** value

```sh
atteler --retrieval-limit <value>
```

### `--retrieval-search`

Search selected retrieval sources under the shared result contract and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --retrieval-search <value>
```

### `--retrieval-source`

Retrieval source to search: memory, file, session, git, adr, vector, agent-memory, or all (repeatable or comma-separated)

- **Type:** value

```sh
atteler --retrieval-source <value>
```

### `--retry-headless`

Retry one terminal headless run by headless ID and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --retry-headless <value>
```

### `--retry-headless-id`

Set the file-name run ID for --retry-headless; defaults to a generated retry ID

- **Type:** string
- **Default:** `""`

```sh
atteler --retry-headless-id <value>
```

### `--review-agent`

Reviewer name for --review-plan/--review-run (repeatable or comma-separated)

- **Type:** value

```sh
atteler --review-agent <value>
```

### `--review-gate`

Required gate check for --review-plan/--review-run (repeatable or comma-separated)

- **Type:** value

```sh
atteler --review-gate <value>
```

### `--review-path`

Path for --review-plan/--review-run review surface (repeatable or comma-separated)

- **Type:** value

```sh
atteler --review-path <value>
```

### `--review-plan`

Print speculative review-agent plan and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --review-plan
```

### `--review-prompt`

Additional review instructions for --review-run

- **Type:** string
- **Default:** `""`

```sh
atteler --review-prompt <value>
```

### `--review-run`

Execute the full review-agent three-round pipeline with real LLM calls and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --review-run
```

### `--review-scan`

Scan the current repository and print a structured review report and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --review-scan
```

### `--route-batch`

Rank model route candidates for batch/cost preference

- **Type:** bool
- **Default:** `false`

```sh
atteler --route-batch
```

### `--route-budget`

Maximum estimated request cost for model routing

- **Type:** value

```sh
atteler --route-budget <value>
```

### `--route-cache-reuse`

Prompt-cache reuse estimate for model routing (0..1)

- **Type:** value

```sh
atteler --route-cache-reuse <value>
```

### `--route-cache-write-tokens`

Estimated prompt-cache write tokens for model routing

- **Type:** value

```sh
atteler --route-cache-write-tokens <value>
```

### `--route-candidate`

Model route candidate spec: provider/model,key=value... (repeatable; keys include input, output, max, capabilities)

- **Type:** value

```sh
atteler --route-candidate <value>
```

### `--route-input-tokens`

Estimated input tokens for model routing

- **Type:** value

```sh
atteler --route-input-tokens <value>
```

### `--route-interactive`

Rank model route candidates for low TTFT

- **Type:** bool
- **Default:** `false`

```sh
atteler --route-interactive
```

### `--route-output-tokens`

Estimated output tokens for model routing

- **Type:** value

```sh
atteler --route-output-tokens <value>
```

### `--route-required-capability`

Required model route capability (repeatable or comma-separated)

- **Type:** value

```sh
atteler --route-required-capability <value>
```

### `--run-lint`

Append the golangci_lint verification gate for --issue-implement

- **Type:** bool
- **Default:** `false`

```sh
atteler --run-lint
```

### `--run-plugin`

Run configured plugin name, or plugin/entrypoint when --plugin-entrypoint is omitted

- **Type:** string
- **Default:** `""`

```sh
atteler --run-plugin <value>
```

### `--run-tests`

Append the go_test verification gate for --issue-implement

- **Type:** bool
- **Default:** `false`

```sh
atteler --run-tests
```

### `--scout-output`

Directory for --scout-run artifacts; defaults to .atteler/runs/scout/<run-id>

- **Type:** string
- **Default:** `""`

```sh
atteler --scout-output <value>
```

### `--scout-run`

Create a local-first feature discovery and roadmap run for this prompt and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --scout-run <value>
```

### `--scout-source`

Local file/dir or URL to include in --scout-run context (repeatable or comma-separated)

- **Type:** value

```sh
atteler --scout-source <value>
```

### `--search-sessions`

Search saved session transcripts and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --search-sessions <value>
```

### `--seed`

Best-effort deterministic seed for providers that support it

- **Type:** value

```sh
atteler --seed <value>
```

### `--sentry`

Sentry issue reference for incident diagnose

- **Type:** string
- **Default:** `""`

```sh
atteler --sentry <value>
```

### `--session`

Session ID or path to continue

- **Type:** string
- **Default:** `""`

```sh
atteler --session <value>
```

### `--session-dir`

Directory for session JSON files

- **Type:** string
- **Default:** `""`

```sh
atteler --session-dir <value>
```

### `--session-id`

Alias for --session

- **Type:** string
- **Default:** `""`

```sh
atteler --session-id <value>
```

### `--session-summary`

Print compact saved session metadata and counts and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --session-summary <value>
```

### `--session-tag`

Add a saved session tag (repeatable or comma-separated)

- **Type:** value

```sh
atteler --session-tag <value>
```

### `--session-title`

Set or update the saved session title

- **Type:** string
- **Default:** `""`

```sh
atteler --session-title <value>
```

### `--show-run`

Multi-agent run ID, latest, review, or speculation to show as YAML on the selected session

- **Type:** string
- **Default:** `""`

```sh
atteler --show-run <value>
```

### `--show-session`

Print saved session details as YAML and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --show-session <value>
```

### `--skill-learning-delete`

Delete one automatically generated skill by slug

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-learning-delete <value>
```

### `--skill-learning-dir`

Automatic skill learning state directory

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-learning-dir <value>
```

### `--skill-learning-disable`

Disable updates for one automatically generated skill by slug

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-learning-disable <value>
```

### `--skill-learning-disable-all`

Disable automatic skill learning in local state

- **Type:** bool
- **Default:** `false`

```sh
atteler --skill-learning-disable-all
```

### `--skill-learning-edit`

Open one automatically generated skill in $VISUAL or $EDITOR by slug

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-learning-edit <value>
```

### `--skill-learning-enable`

Enable updates for one automatically generated skill by slug

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-learning-enable <value>
```

### `--skill-learning-enable-all`

Enable automatic skill learning in local state

- **Type:** bool
- **Default:** `false`

```sh
atteler --skill-learning-enable-all
```

### `--skill-learning-list`

List automatically generated skills and learning status

- **Type:** bool
- **Default:** `false`

```sh
atteler --skill-learning-list
```

### `--skill-learning-show`

Print one automatically generated skill by slug

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-learning-show <value>
```

### `--skill-learning-skill-dir`

Directory containing automatically generated skills

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-learning-skill-dir <value>
```

### `--skill-max-steps`

Maximum repeated sequence length for --skill-step suggestions

- **Type:** value

```sh
atteler --skill-max-steps <value>
```

### `--skill-min-occurrences`

Minimum repeated occurrences for --skill-step suggestions

- **Type:** value

```sh
atteler --skill-min-occurrences <value>
```

### `--skill-review-only`

Print generated --skill-save-dir skill diff without writing files

- **Type:** bool
- **Default:** `false`

```sh
atteler --skill-review-only
```

### `--skill-save-dir`

Persist accepted --skill-step suggestion as a reviewed skill directory

- **Type:** string
- **Default:** `""`

```sh
atteler --skill-save-dir <value>
```

### `--skill-step`

Observed action for skill suggestion; optionally append |prompt=...|tool=...|input=...|output=...|verify=...|stop=...

- **Type:** value

```sh
atteler --skill-step <value>
```

### `--spawn-agent`

Spawn sub-agent spec: id|agent|prompt or agent|prompt (repeatable)

- **Type:** value

```sh
atteler --spawn-agent <value>
```

### `--spawn-binary`

Atteler binary for --spawn-agent; defaults to the current executable

- **Type:** string
- **Default:** `""`

```sh
atteler --spawn-binary <value>
```

### `--spawn-cancel-on-failure`

Cancel same-wave/sibling child work after the first failure

- **Type:** bool
- **Default:** `false`

```sh
atteler --spawn-cancel-on-failure
```

### `--spawn-cost-budget-micros`

Aggregate estimated cost budget in micros for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-cost-budget-micros <value>
```

### `--spawn-dry-run`

Print --spawn-agent invocations without executing them

- **Type:** bool
- **Default:** `false`

```sh
atteler --spawn-dry-run
```

### `--spawn-ledger`

JSON execution ledger path for --spawn-agent/--async-run; defaults under .atteler/runs

- **Type:** string
- **Default:** `""`

```sh
atteler --spawn-ledger <value>
```

### `--spawn-max-concurrency`

Maximum concurrent child processes for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-max-concurrency <value>
```

### `--spawn-output-budget-bytes`

Aggregate stdout/stderr byte budget for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-output-budget-bytes <value>
```

### `--spawn-resume`

Resume matching successful children from --spawn-ledger without rerunning them

- **Type:** bool
- **Default:** `false`

```sh
atteler --spawn-resume
```

### `--spawn-retries`

Retry count per child for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-retries <value>
```

### `--spawn-retry-backoff-seconds`

Retry backoff in seconds for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-retry-backoff-seconds <value>
```

### `--spawn-task-timeout-seconds`

Per-child timeout in seconds for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-task-timeout-seconds <value>
```

### `--spawn-timeout-seconds`

Overall timeout in seconds for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-timeout-seconds <value>
```

### `--spawn-token-budget`

Aggregate estimated prompt-token budget for --spawn-agent/--async-run

- **Type:** value

```sh
atteler --spawn-token-budget <value>
```

### `--speculate-agent`

Agent name for --speculate-plan/--speculate-run (repeatable or comma-separated)

- **Type:** value

```sh
atteler --speculate-agent <value>
```

### `--speculate-gate`

Required explicit gate check for --speculate-plan/--speculate-run (repeatable or comma-separated)

- **Type:** value

```sh
atteler --speculate-gate <value>
```

### `--speculate-plan`

Print a speculative three-round execution plan and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --speculate-plan
```

### `--speculate-prompt`

Base task prompt for --speculate-plan estimates or --speculate-run execution

- **Type:** string
- **Default:** `""`

```sh
atteler --speculate-prompt <value>
```

### `--speculate-run`

Execute the full speculative three-round pipeline with real LLM calls and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --speculate-run
```

### `--state-diagnostics`

Print persisted interactive state diagnostics and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --state-diagnostics
```

### `--status-headless`

Show one headless run status by headless ID and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --status-headless <value>
```

### `--stdin`

Append stdin to a one-shot prompt

- **Type:** bool
- **Default:** `false`

```sh
atteler --stdin
```

### `--stream-headless`

Stream one headless run log by headless ID and exit when it finishes

- **Type:** string
- **Default:** `""`

```sh
atteler --stream-headless <value>
```

### `--task-add`

Add a persistent agent task/TODO item and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --task-add <value>
```

### `--task-agent`

Agent for --task-add or --task-complete

- **Type:** string
- **Default:** `""`

```sh
atteler --task-agent <value>
```

### `--task-assign`

Assign a task as id:agent and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --task-assign <value>
```

### `--task-complete`

Mark a task complete by ID and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --task-complete <value>
```

### `--task-file`

JSON task-list file; defaults to .atteler/tasks.json

- **Type:** string
- **Default:** `""`

```sh
atteler --task-file <value>
```

### `--task-id`

Optional stable ID for --task-add

- **Type:** string
- **Default:** `""`

```sh
atteler --task-id <value>
```

### `--task-list`

List persistent agent task/TODO items and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --task-list
```

### `--temperature`

Override request temperature

- **Type:** value

```sh
atteler --temperature <value>
```

### `--top-p`

Override request nucleus sampling value (0..1)

- **Type:** value

```sh
atteler --top-p <value>
```

### `--tournament`

Enable shared tournament/variant comparison for scout or autoresearch

- **Type:** bool
- **Default:** `false`

```sh
atteler --tournament
```

### `--trusted-source`

Trusted source host or domain for research (repeatable or comma-separated)

- **Type:** value

```sh
atteler --trusted-source <value>
```

### `--update-changelog`

Tell the issue implementation worker to update the changelog when relevant

- **Type:** bool
- **Default:** `false`

```sh
atteler --update-changelog
```

### `--update-docs`

Tell the issue implementation worker to update docs when relevant

- **Type:** bool
- **Default:** `false`

```sh
atteler --update-docs
```

### `--validate-config`

Validate merged YAML/JSON config and importer warnings, then exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --validate-config
```

### `--variants`

Number of tournament variants for scout or autoresearch

- **Type:** value

```sh
atteler --variants <value>
```

### `--vector-base-url`

Embedding API base URL

- **Type:** string
- **Default:** `""`

```sh
atteler --vector-base-url <value>
```

### `--vector-chunk-max-runes`

Maximum runes per indexed vector chunk

- **Type:** value

```sh
atteler --vector-chunk-max-runes <value>
```

### `--vector-chunk-overlap-runes`

Overlap runes between indexed vector chunks

- **Type:** value

```sh
atteler --vector-chunk-overlap-runes <value>
```

### `--vector-fallback`

Embedding failure policy: fail or lexical

- **Type:** string
- **Default:** `""`

```sh
atteler --vector-fallback <value>
```

### `--vector-index`

UTF-8 file to add to the persisted vector index (repeatable or comma-separated)

- **Type:** value

```sh
atteler --vector-index <value>
```

### `--vector-limit`

Maximum vector search results

- **Type:** value

```sh
atteler --vector-limit <value>
```

### `--vector-model`

Embedding model name

- **Type:** string
- **Default:** `""`

```sh
atteler --vector-model <value>
```

### `--vector-provider`

Embedding provider label; currently ollama-compatible

- **Type:** string
- **Default:** `""`

```sh
atteler --vector-provider <value>
```

### `--vector-search`

Search a local vector index and exit

- **Type:** string
- **Default:** `""`

```sh
atteler --vector-search <value>
```

### `--vector-store`

JSON vector index path; defaults to vector.index_path or .atteler/vector-index.json

- **Type:** string
- **Default:** `""`

```sh
atteler --vector-store <value>
```

### `--vector-timeout-seconds`

Embedding request timeout in seconds

- **Type:** value

```sh
atteler --vector-timeout-seconds <value>
```

### `--vectorizer`

Vector retrieval mode: lexical or embedding

- **Type:** string
- **Default:** `""`

```sh
atteler --vectorizer <value>
```

### `--version`

Print version and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --version
```

### `--watch-baseline`

Compare watch findings against a baseline JSON file from an earlier watch JSON scan

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-baseline <value>
```

### `--watch-baseline-ref`

Compare watch findings against the git merge-base between HEAD and this ref

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-baseline-ref <value>
```

### `--watch-gate`

Fail watch scan/loop when new unsuppressed findings meet the gate severity

- **Type:** bool
- **Default:** `false`

```sh
atteler --watch-gate
```

### `--watch-gate-min-severity`

Minimum severity for --watch-gate; defaults to high

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-gate-min-severity <value>
```

### `--watch-github-api-url`

GitHub API endpoint for --watch-upsert-issues; defaults to https://api.github.com

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-github-api-url <value>
```

### `--watch-github-repository`

GitHub repository owner/name for --watch-upsert-issues

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-github-repository <value>
```

### `--watch-github-token`

GitHub token for --watch-upsert-issues; defaults to GITHUB_TOKEN or GH_TOKEN

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-github-token <value>
```

### `--watch-interval-seconds`

Seconds between --watch-loop scans

- **Type:** value

```sh
atteler --watch-interval-seconds <value>
```

### `--watch-issue-label`

GitHub issue label for --watch-upsert-issues (repeatable or comma-separated; defaults to quality,watch)

- **Type:** value

```sh
atteler --watch-issue-label <value>
```

### `--watch-issue-min-severity`

Minimum severity for --watch-upsert-issues; defaults to high

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-issue-min-severity <value>
```

### `--watch-json`

Emit --watch-scan findings as JSON

- **Type:** bool
- **Default:** `false`

```sh
atteler --watch-json
```

### `--watch-large-file-bytes`

Large-file byte threshold for --watch-scan

- **Type:** value

```sh
atteler --watch-large-file-bytes <value>
```

### `--watch-loop`

Continuously scan the current repository for background-agent health findings

- **Type:** bool
- **Default:** `false`

```sh
atteler --watch-loop
```

### `--watch-max-iterations`

Maximum --watch-loop scans before exiting; omit to run until interrupted

- **Type:** value

```sh
atteler --watch-max-iterations <value>
```

### `--watch-rules`

Configure watch scan rules from a JSON file with rule_id, severity/help/owner overrides, or disabled=true

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-rules <value>
```

### `--watch-scan`

Scan the current repository for background-agent health findings and exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --watch-scan
```

### `--watch-suppressions`

Apply watch finding suppressions from a JSON file with required reasons

- **Type:** string
- **Default:** `""`

```sh
atteler --watch-suppressions <value>
```

### `--watch-upsert-issues`

Create or update GitHub issues for new unsuppressed watch findings meeting --watch-issue-min-severity

- **Type:** bool
- **Default:** `false`

```sh
atteler --watch-upsert-issues
```

### `--worktree`

Isolate session in a git worktree

- **Type:** bool
- **Default:** `false`

```sh
atteler --worktree
```

### `--worktree-auto-merge`

Opt in to reviewed worktree auto-merge on session exit

- **Type:** bool
- **Default:** `false`

```sh
atteler --worktree-auto-merge
```

### `--worktree-merge-override`

Explicitly allow worktree merge without verification commands

- **Type:** bool
- **Default:** `false`

```sh
atteler --worktree-merge-override
```

### `--worktree-verify-command`

Verification command to run inside the session worktree before merge (repeatable)

- **Type:** value

```sh
atteler --worktree-verify-command <value>
```
