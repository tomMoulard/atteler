# Reviewed fixtures

Only commit fixtures that are intentionally curated and redacted. Name committed
fixtures with `.fixture.json`, `.fixture.yaml`, or `.fixture.yml` so the
repository `.gitignore` allows them.

Raw `atteler eval record-response` files and ad-hoc actual outputs are ignored
by default. Review and rename them before committing.
