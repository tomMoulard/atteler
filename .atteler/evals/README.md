# Reviewed eval suites

Commit structured eval suites as `*.eval.yaml`, `*.eval.yml`, or `*.eval.json`.
Keep generated reports such as `.atteler/eval-report.json` ignored unless they
are redacted and moved to a reviewed documentation location.
