- [x] show token in/cached/out when closing the harness
- [x] all of the debug cli flags should be env vars that starts with `DEBUG_`
- [x] actually run async agent waves in parallel with `--async-run` (not only `--async-plan` previews)
- [x] actually run a speculative three-round session (today only `--speculate-plan` previews)
- [x] apply feedback proposals back into agent config + history log
- [x] enforce per-task budget hard-stop using `pkg/modelroute` signals
- [x] prompt-cache prefix reuse across speculative branches
- [x] allow choosing the effort when selecting the llm model/provider
- [x] agents spawning sub-agents
- [x] per-agent persistent memory bound to `pkg/vector`
- [x] skill acceptance + persistence (suggestion exists, no save flow)
- [x] MCP client invocation (manifest validation exists)
- [x] LSP / tree-sitter code intel beyond Go AST
- [x] LSP workspace symbol lookup
- [x] background scan convention drift checks
- [x] hook event discovery CLI
- [x] session list filtering by tag
- [x] code-merge agent for sandbox artifact aggregation
- [x] continuous background-agent loop (today only one-shot `--watch-scan`)
- [x] run bash commands
- [x] proper context propagation. Only one context.Background() in the main.go file, everything else should be context-aware and receive a context from its caller.
- [x] providerless local inspection commands avoid credential/network side effects
- [x] incorporate ollama llm models so that the harness can use local llm models for inference without needing to call out to external APIs. This will be especially useful for testing and development of the harness itself, as well as for users who want to run agents without relying on third-party providers.
- [x] auto-start the local Ollama daemon by forking `ollama serve` when a selected local Ollama run finds no daemon available.
- [x] add machine-readable JSON output for watch scans and hook event inventory.
- [x] add an offline doctor/readiness inventory that avoids provider health checks.
- [x] expose the Claude Code Bash tool so one-shot shell-command requests can run through `claude --print`.
- [x] aggregate agent performance summaries across sessions
- [x] review-agent speculative plan preview
- [x] headless mode for running sessions without the CLI output (e.g. for CI or as a library), and a corresponding `--output json` flag for machine-readable session logs and results; add a cli arg to list the active headless sessions and stream their logs in real time
- [x] when I open a new attelier prompt, I should be able to press the up arrow key to have the last prompt as input, and I should be able to cycle through previous prompts with repeated presses of the up/down arrow key.
- [x] autocompletion in the input prompt. I want to be able to press tab to select the autocompletion of the prompt that was generated by the model. The autocompketion should apear in gray text after the cursor, and if I press tab, it should be added to the prompt input.
- [x] add a keybind to revamp the prompt to make it better (e.g. by adding more detail, or by making it more concise). This would trigger a re-generation of the prompt by the model, and the new prompt would replace the current input. If the user does <ctrl-z> after the revamp, it should revert to the previous prompt.
- [x] allow the user to type and enqueue follow-up prompts while an agent is still running, and have those prompts be automatically sent to the agent as new tasks once it finishes its current task. This would allow for a more interactive and dynamic experience, where the user can provide additional guidance or requests to the agent without having to wait for it to finish its current task.
- [x] I do not see the autocompletion suggestions in the input prompt. I want to be able to see the autocompletion suggestions in gray text after the cursor, so that I can decide whether to accept them by pressing tab or ignore them and keep typing.

```bash
> make run
go run ./cmd/atteler
2026/05/04 23:38:29 llm: ollama skipped: ollama: models request: Get "http://127.0.0.1:11434/api/tags": dial tcp 127.0.0.1:11434: connect: connection refused
atteler  Ctrl+D to quit, Ctrl+O to pick model
  Config: /Users/tommoulard/.codex/config.toml, /Users/tommoulard/.claude/settings.json, /Users/tommoulard/.config/opencode/opencode.json, /Users/tommoulard/.config/opencode/oh-my-openagent.json, /Users/tommoulard/.config/opencode/agent/code-quality-reviewer.md, /Users/tommoulard/.config/opencode/agent/cr.md, /Users/tommoulard/.config/opencode/agent/documenter.md, /Users/tommoulard/.config/opencode/agent/error-analyzer.md, /Users/tommoulard/.config/opencode/agent/performance-analyzer.md, /Users/tommoulard/.config/opencode/agent/prompt-engineer.md, /Users/tommoulard/.config/opencode/agent/review-claude.md, /Users/tommoulard/.config/opencode/agent/review-gemini.md, /Users/tommoulard/.config/opencode/agent/review-openai.md, /Users/tommoulard/.config/opencode/agent/review.md, /Users/tommoulard/.config/opencode/agent/synthflow-debug.md, /Users/tommoulard/.config/opencode/agent/test-engineer.md, /Users/tommoulard/forge/.forge.toml
  Session: 20260504-213829-a2d797f5 (/Users/tommoulard/go/src/github.com/tommoulard/atteler/.atteler/sessions/20260504-213829-a2d797f5.json)
  Connected providers: anthropic, claude-code, codex
  Agents: code-quality-reviewer, cr, documenter, error-analyzer, performance-analyzer, prompt-engineer, review, synthflow-debug, test-engineer
fatal error: all goroutines are asleep - deadlock!

goroutine 1 [select]:
github.com/charmbracelet/bubbletea.(*Program).Send(...)
        /Users/tommoulard/go/pkg/mod/github.com/charmbracelet/bubbletea@v1.3.10/tea.go:775
main.(*tuiEventWriter).Write(0x5610c7494c30?, {0x5610c7740800?, 0x57, 0x100349150?})
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/cmd/atteler/main.go:2171 +0xe0
fmt.Fprintln({0x1009a58b8, 0x5610c7714018}, {0x5610c77d0448, 0x1, 0x1})
        /opt/homebrew/Cellar/go/1.26.2/libexec/src/fmt/print.go:306 +0x74
github.com/tommoulard/atteler/pkg/events.(*Logger).Log(0x5610c770a380, {0x0, {0x12285890, 0xee18b08d5, 0x0}, {0x10055a299, 0xd}, {0x5610c73a49a8, 0x18}, {0x5610c785e5b0, ...}, ...})
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/pkg/events/logger.go:38 +0x94
github.com/tommoulard/atteler/pkg/events.(*Runner).Emit(0x5610c770a390, {0x1009aa410, 0x100a763c0}, {0x0, {0x12285890, 0xee18b08d5, 0x0}, {0x10055a299, 0xd}, {0x5610c73a49a8, ...}, ...})
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/pkg/events/events.go:144 +0xe4
main.emitHookWarning({0x1009aa410?, 0x100a763c0?}, 0x0?, {0x0, {0x0, 0x0, 0x0}, {0x10055a299, 0xd}, {0x5610c73a49a8, ...}, ...})
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/cmd/atteler/main.go:2137 +0x70
main.runInteractive({_, _}, {{{0x3865418, 0xee18b08d5, 0x0}, {0x3865418, 0xee18b08d5, 0x0}, {0x5610c73a49a8, 0x18}, ...}, ...})
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/cmd/atteler/main.go:4119 +0xfa0
main.runWithState({_, _}, {{0x0, 0x0}, {0x0, 0x0}, {0x0, 0x0}, {0x0, 0x0}, ...}, ...)
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/cmd/atteler/main.go:3398 +0x17c
main.run({0x1009aa410?, 0x100a763c0?})
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/cmd/atteler/main.go:3123 +0x338
main.main()
        /Users/tommoulard/go/src/github.com/tommoulard/atteler/cmd/atteler/main.go:2893 +0x2c
exit status 2
make: *** [run] Error 1
```

- [x] have a TODO list that agents/subagents can interact with, so that they can add items to the TODO list for themselves or other agents to complete, and check off items as they are completed. This would help agents keep track of their tasks and progress, and allow for better coordination and delegation among multiple agents working on the same session.
